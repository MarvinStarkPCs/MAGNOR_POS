<Window x:Class="MAGNOR_POS.Views.TableManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gestión de Mesas y Zonas"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#F9FAFB">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#D1D5DB"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#DC2626"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B91C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#10B981"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#146e39" Padding="20">
            <Grid>
                <StackPanel>
                    <TextBlock Text="🍽️ Gestión de Mesas y Zonas"
                               FontSize="24" FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="Configure las zonas y mesas de su restaurante"
                               FontSize="14" Foreground="#E0E0E0" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Content="🔄 Actualizar"
                        Style="{StaticResource ActionButton}"
                        HorizontalAlignment="Right"
                        Command="{Binding RefreshCommand}"/>
            </Grid>
        </Border>

        <!-- Statistics Cards -->
        <Grid Grid.Row="1" Margin="20,15,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Total de Mesas" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock Text="{Binding TotalTables}" FontSize="28" FontWeight="Bold"
                               Foreground="#146e39" Margin="0,5"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Disponibles" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock Text="{Binding AvailableTables}" FontSize="28" FontWeight="Bold"
                               Foreground="#10B981" Margin="0,5"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Ocupadas" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock Text="{Binding OccupiedTables}" FontSize="28" FontWeight="Bold"
                               Foreground="#EF4444" Margin="0,5"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Reservadas" FontSize="12" Foreground="#6B7280"/>
                    <TextBlock Text="{Binding ReservedTables}" FontSize="28" FontWeight="Bold"
                               Foreground="#F59E0B" Margin="0,5"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Zones Panel -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Zones Header -->
                    <TextBlock Grid.Row="0" Text="📍 Zonas del Restaurante"
                               FontSize="16" FontWeight="Bold"
                               Foreground="#146e39" Margin="0,0,0,10"/>

                    <!-- Zones List -->
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Zones}"
                             SelectedItem="{Binding SelectedZone}"
                             BorderThickness="1" BorderBrush="#E5E7EB"
                             Background="#F9FAFB">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12"
                                               Foreground="#6B7280" Margin="0,2,0,0"/>
                                    <TextBlock FontSize="11" Foreground="#9CA3AF" Margin="0,2,0,0">
                                        <Run Text="Mesas: "/>
                                        <Run Text="{Binding Tables.Count, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Zone Actions -->
                    <StackPanel Grid.Row="2" Margin="0,10,0,0">
                        <Button Content="➕ Nueva Zona" Style="{StaticResource SuccessButton}"
                                Command="{Binding AddZoneCommand}" Margin="0,0,0,5"/>
                        <Button Content="✏️ Editar Zona" Style="{StaticResource ActionButton}"
                                Command="{Binding EditZoneCommand}" Margin="0,0,0,5"/>
                        <Button Content="🗑️ Eliminar Zona" Style="{StaticResource DangerButton}"
                                Command="{Binding DeleteZoneCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tables Panel -->
            <Border Grid.Column="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1"
                    CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Tables Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock FontSize="16" FontWeight="Bold" Foreground="#146e39">
                            <Run Text="🪑 Mesas"/>
                            <Run Text=" - " Foreground="#6B7280"/>
                            <Run Text="{Binding SelectedZone.Name, Mode=OneWay}" Foreground="#6B7280"/>
                        </TextBlock>
                    </Grid>

                    <!-- Tables DataGrid -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Tables}"
                              SelectedItem="{Binding SelectedTable}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              GridLinesVisibility="None"
                              HeadersVisibility="Column"
                              Background="White"
                              RowHeight="45"
                              FontSize="13">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F9FAFB"/>
                                <Setter Property="Foreground" Value="#374151"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E5E7EB"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F9FAFB"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E0F2FE"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Número" Binding="{Binding TableNumber}" Width="100"/>
                            <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacity}" Width="100"/>
                            <DataGridTemplateColumn Header="Estado" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="0">
                                                            <Setter Property="Background" Value="#D1FAE5"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="1">
                                                            <Setter Property="Background" Value="#FEE2E2"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="2">
                                                            <Setter Property="Background" Value="#FEF3C7"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="11" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="0">
                                                                <Setter Property="Text" Value="DISPONIBLE"/>
                                                                <Setter Property="Foreground" Value="#065F46"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="1">
                                                                <Setter Property="Text" Value="OCUPADA"/>
                                                                <Setter Property="Foreground" Value="#991B1B"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="2">
                                                                <Setter Property="Text" Value="RESERVADA"/>
                                                                <Setter Property="Foreground" Value="#92400E"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Table Actions -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="➕ Nueva Mesa" Style="{StaticResource SuccessButton}"
                                Command="{Binding AddTableCommand}"/>
                        <Button Content="✏️ Editar Mesa" Style="{StaticResource ActionButton}"
                                Command="{Binding EditTableCommand}"/>
                        <Button Content="🗑️ Eliminar Mesa" Style="{StaticResource DangerButton}"
                                Command="{Binding DeleteTableCommand}"/>
                    </StackPanel>

                    <!-- Loading Indicator -->
                    <Border Grid.Row="0" Grid.RowSpan="3" Background="#80FFFFFF"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="⏳" FontSize="48" HorizontalAlignment="Center"/>
                            <TextBlock Text="Cargando..." FontSize="16" Foreground="#6B7280"
                                       Margin="0,10,0,0" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
